import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fluxodorotimer.ionicangular',
  appName: 'fluxodoro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
