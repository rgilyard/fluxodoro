import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fluxodoro-timer.ionic-angular',
  appName: 'fluxodoro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
